{% extends 'backend.html.twig' %}

{% block title %}{{ parent () }} Gestion du reglement{% endblock %}

{% block body %}
    <section class="content-header">
        <h1>Gestion du reglement</h1>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ absolute_url(path('backend'))}}"><i class="fa fa-home"></i> Accueil</a></li>
            <li class="breadcrumb-item"><a href="{{ absolute_url(path('backend_reglement_index'))}}">reglement</a></li>
            <li class="breadcrumb-item active">Liste </li>
        </ol>
    </section>

    <!-- Main content -->
    <section class="content">

        <div class="box box-warning">
            <div class="box-header text-right">
                <a href="{{ absolute_url(path('backend_reglement_new'))}}" class="btn btn-social btn-primary"><i class="fa fa-plus"></i>Ajouter un reglement</a>
            </div>

            <div class="row">
                {% for reglement in reglements %}
                    <div class="col-md-12 col-lg-3">
                        <div class="box box-default" style="height: 600px">
                            <img class="card-img-top img-responsive" src="{{ vich_uploader_asset(reglement, 'imageFile') }}" alt="{{ reglement.titre }}">
                            <div class="box-body">
                                <h4 class="card-title">
                                    {% if reglement.statut %}
                                        <span class="badge badge-success">Actif</span>
                                    {% else %}
                                        <span class="badge badge-danger">Désactivé</span>
                                    {% endif %}
                                    {{ reglement.titre }}
                                </h4>
                                <p class="card-text text-justify">{{ reglement.resume|raw }}</p>
                                <div class="btn-group" role="group" aria-label="Basic example">
                                    <a href="{{ absolute_url(path('backend_reglement_edit', {'slug': reglement.slug})) }}" >
                                        <button type="button" class="btn btn-primary"><i class="fa fa-edit"></i> Modifier</button>
                                    </a>
                                    <a href="{{ absolute_url(path('backend_reglement_show', {'slug': reglement.slug})) }}" >
                                        <button type="button" class="btn btn-primary"><i class="fa fa-eye"></i> Consulter</button>
                                    </a>
                                </div>
                            </div>

                        </div>
                        <!-- /.box -->
                    </div>
                {% else %}

                {% endfor %}
            </div>

    </section>
{% endblock %}
